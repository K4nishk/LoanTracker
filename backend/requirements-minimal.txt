# Minimal requirements for LoanTracker MVP1
# Use this if requirements.txt fails to install
# Encryption and pandas are optional - installed separately

# Core API framework
fastapi>=0.100.0
uvicorn>=0.20.0

# Data validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# File handling
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Logging
structlog>=23.0.0

# HTTP client for testing
httpx>=0.24.0

# Optional dependencies (installed separately):
# - cryptography>=41.0.0  (for encryption support)
# - pandas>=2.0.0         (for better CSV performance)
